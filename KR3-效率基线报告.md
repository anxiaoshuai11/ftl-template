# KR3 · 完成效率基线指标定义与数据采集，输出初版基线报告

> 归属 OKR **O2**：完成前端 AI 样板间核心能力落地，支撑组内规范沉淀与效率基线测算（截止 09-30）
> 权重：33.3%
> 样本案例：订单+交易合并小票（见 [KR1 可复用案例](https://zath6evqnvz.feishu.cn/wiki/ATglwvtpAidD85kHEjfcPkUQnVe)）

**本 KR 交付物：** 指标定义 + 采集表 + 初版基线结论，**含效率与质量两个维度**。

配套文档：[KR1 可复用案例](https://zath6evqnvz.feishu.cn/wiki/ATglwvtpAidD85kHEjfcPkUQnVe) · [KR2 实践文档](https://zath6evqnvz.feishu.cn/wiki/VJNGwzbzyitgr1kONzlcWCqKnQe)

---

## 0. 三条记账原则

**一、工时分两笔账。** 本次同时做了两件事：完成需求，以及把流程沉淀成工具。
工具是**一次性投入**，会摊薄到后续每个需求上。混成一笔算会同时误导两个方向——
算进去显得 AI 没省多少，不算又不诚实。所以分开记，并写明摊薄逻辑。

**二、只报工时不够。**「快」如果伴随「漏测」就不是效率提升，只是把成本推到线上。
所以效率之外必须有质量基线，且质量数据要**可复现、可验证**，不能是自我评价。

**三、上报用全周期，不用编码切片。** 只记「打开对话到改完文件」，会把读需求、
grill-me 口径锁定漏掉，节省率会被编码段拉得很高，组内不可信。
**需求理解两边都要记；grill-me 必须整段计入，不能只记当晚锁定那 1 小时。**
全周期提效按约 **20%** 上报，编码切片数字只作内部对照、不上 OKR。

---

## 1. 指标定义

### 效率类

| 指标 | 定义 | 采集方式 |
|------|------|----------|
| 手写基线工时 | 假设无 AI，按工作包估算完成同等范围的人时（含读需求、自己拆口径） | 拆包估算（本案例 **16.5h**） |
| AI 辅助实测工时 | 用 AI 完成同等范围的墙钟人时（含读需求、grill-me、纠错，不含等后端） | 实填（本案例 **13.2h**） |
| 工具建设工时 | 沉淀自测工具、skill、GitLab 脚本的一次性投入 | 单独记，不计入本次需求 |
| 工时节省率 | `(手写基线 - AI 实测) / 手写基线` | 计算得出（本案例 **20.0%**） |
| 返工次数 | 因需求理解偏差或生成错误导致的明显返工轮数 | 人工计数 |

### 质量类

| 指标 | 定义 | 本案例实测 |
|------|------|------------|
| 场景覆盖数 | 自测跑了多少组数据场景 | **15** |
| 顶层分区覆盖率 | 可判定的分区级分支被覆盖的比例 | **16/16（100%）** |
| 交付前错误数 | 自测发现的错误级问题（提交前已清零） | **0** |
| 未决分支数 | 静态分析判不了、需人工确认的分支 | **2**（已人工看预览确认） |
| 工具有效性 | 坏数据是否真的会被报出来 | **已验证**，见第 3 节 |

### 计时边界

- **计入**：读飞书需求与旧模版 / JSON 模型、grill-me 全段（含落盘 spec/ADR）、后续口径澄清、生成/修改 FTL 与 JSON、自测、写 KR 材料
- **不计入**：等产品答疑空窗、等后端真数据、短信联调环境问题
- **单独计**：工具与 skill 的建设工时

### 对照口径的局限（汇报时必须带上）

手写基线是**本人拆包估时**，不是第二人盲做，所以这是「估时 vs 实测」而非严格 A/B。
节省率只能当趋势参考，不能当精确结论。全周期 20% 是把需求理解摊进去之后的上报口径；
编码段看起来省得更多，但那一截单独拿出来会高估 AI 对整单需求的贡献。

---

## 2. 数据采集表

### 2.0 口径对照（为什么从 63.6% 改到 20%）

原表只记 8/20–8/21 编码墙钟，grill 只写了当晚 16:47–17:45 的 **1.0h**，
读飞书需求、两份旧 FTL、JSON 数据模型、三轮 grill 落盘、后续胶囊/图稿澄清都没单列。
编码切片是 **14.0h vs 5.1h = 63.6%**，看起来提效过多，组内容易被质疑。

按组长要求改成全周期后，需求理解两边对称补上，grill-me 整段计入 AI 侧；
编码仍按原墙钟加核对时间，不上报切片数字。

| 口径 | 手写(h) | AI(h) | 节省率 | 用途 |
|------|---------|-------|--------|------|
| 编码切片（原表，已废弃） | 14.0 | 5.1 | 63.6% | 内部对照，**不上 OKR** |
| **全周期（含需求理解 + grill-me）** | **16.5** | **13.2** | **20.0%** | **上报口径** |

### 2.1 本次需求工时（全周期）

口径：手写基线 **16.5h**（原 14h 实现估时 + 补齐读需求与自己拆口径）；AI 实测 **13.2h**。

| 工作包 | 手写基线(h) | AI 实测(h) | 返工次数 | 备注 |
|--------|-------------|------------|----------|------|
| 读飞书需求 + 两份旧 FTL + JSON 数据模型 + 砍清单 | 2.0 | 2.5 | 0 | 原表只记 1.2 / 1.0；读文档 AI 几乎不省，AI 侧还要对照两份旧模版喂给 grill |
| grill-me 口径锁定（三轮 Q1–Q15 + 落盘 spec/ADR） | 1.5 | 2.5 | 0 | 手写按「自己拆口径 + 问产品」；AI 含全文落盘，不止当晚锁定 1h |
| 后续口径澄清（胶囊更正、图稿与口头冲突） | 0.5 | 1.0 | 0 | 图稿 `Dine In / A003` 与口头不一致，开工前后都在对齐 |
| HTML/CSS 骨架（六段布局） | 3.0 | 1.5 | 1 | 首版未按 `tr/td`，当晚整页重写；含目视核对 |
| FreeMarker 绑定（含 meal child） | 3.5 | 1.6 | 0 | 与骨架同次生成，含套餐 child 与字段核对 |
| 金额 + Tax&Fee 弹窗 | 1.8 | 1.2 | 1 | 8/21 对照 TaxesFeesLine 重做合并 + 弹层 |
| 支付 `tradeInfo` | 0.7 | 0.4 | 0 | 只用 `tradeInfo`，无刷卡联 |
| Demo JSON + 字段对照 | 1.2 | 0.8 | 0 | 拼盘 Demo + `taxFeeDetailInfo` 占位说明 |
| 空数据/边界自测 | 1.8 | 1.2 | 0 | 预览目视 + 跑场景套件；建工具工时记 2.2 |
| 提示词与 KR 文档整理 | 0.5 | 0.5 | 0 | KR 初稿 + week3 粘贴稿 |
| **合计** | **16.5** | **13.2** | **2** | 节省率 **(16.5 − 13.2) / 16.5 = 20.0%** |

为什么全周期会被摊到 20%：读需求、grill-me、后续澄清这三行合计，
手写 **4.0h**、AI **6.0h**，这一段 AI 不但不省、还多花在口径锁定上；
真正省时间的是后面写模版。编码快、理解慢，整单就不能按编码切片报。

### 2.2 工具建设工时（一次性投入）

| 工作包 | 实测(h) | 说明 |
|--------|---------|------|
| FTL 静态自测工具（扫描 / 条件求值 / 报告） | | 一次性 |
| 场景套件与回归对比工具 | | 一次性 |
| GitLab 助手（MR、附件上传、分支命名推断） | | 一次性 |
| 三个 Cursor skill 与工具仓 README | | 一次性 |
| **合计** | **（待实填）** | 摊薄到后续每个同类需求 |

**摊薄口径：** 工具投入 ÷ 预计受益需求数 = 每需求分摊工时。
按组内同类小票需求一年若干个来估，第 2 个需求开始边际成本就接近 0。

填写说明：

- **AI 实测**：墙钟从打开需求文档 / 开始 grill 算起，含读旧模版、生成等待与纠错，精确到 0.1h。隔夜空窗、等产品答疑、等后端真数据**不计**。工具与 skill 建设记第 2.2 节，不摊进本表。
- **grill-me**：三轮问答 + 落盘 `grill-me-conversation.md` / spec / ADR **整段计入**，不得只记最后一次锁定会话。
- **返工次数**：因需求理解偏差或生成错误，导致**整段推倒重来 / 按新口径重写**才 +1。开工前的口径澄清、措辞微调、看预览后的小改**不计**。
- **本表来源**：需求理解与 grill 按对话与落盘材料还原；编码段按 8/20–8/21 活跃墙钟加核对时间。不按 20% 倒推编码行，20% 是全周期加总后的结果。

---

## 3. 质量基线（可复现，非自我评价）

这一节的数字都能重新跑出来，命令在 [Shawn/ftl-util](http://gitlab.zbspos.com/Shawn/ftl-util) 的 README 里。

### 3.1 交付物质量

15 个场景（仅税 / 仅费 / 无税无费 / 无顾客 / 无支付 / 有无 Logo / OO 取餐码 /
堂食桌台 / 整单备注 / 三种分区开关 / 单号字符串兼容）全部通过，
错误 0、告警 0，可判定顶层分区合并覆盖 100%。

另有 2 个分支条件依赖自定义函数返回值，静态分析判不了，**已标注为「判不了」而非「通过」**，
并人工看预览确认。这条很重要：**把判不了当成测过了，是这类工具最容易造的假。**

### 3.2 工具本身的有效性

一个只会报绿的工具没有价值，所以做了三组反向验证：

| 验证 | 手法 | 结果 |
|------|------|------|
| 坏数据能否被抓出 | 用故意做坏的 fixture 跑 | 退出码 1，报出错误 1、告警 5 |
| 回归能否被拦截 | 故意加一个取不到值的 `${}` 和一个新分支 | 全部拦下，并标出新分支需补用例 |
| 会不会误报 | 只插入空行、不改语义 | 报 0 个新增问题，退出码 0 |

正常数据报通过、坏数据报失败、纯位移不误报，三头都对，报告才有意义。

### 3.3 交付卫生

MR 变更文件列表**只有 1 个 `.ftl`**，自测报告与预览 HTML 走 GitLab 附件区，
评审能看但不入库。用 `git diff --name-only main..<分支>` 可核。

---

## 4. 初版基线报告（可直接贴 OKR 进展）

**样本**：订单+交易合并小票（FreeMarker 模版，约 1 个中等前端需求切片），
交付物 [print-util !88](http://gitlab.zbspos.com/posbee-microservice/print-util/-/merge_requests/88)。

| 项 | 结论 |
|----|------|
| 手写基线 | 16.5 人时（约 2.1 人天；含读需求与自己拆口径） |
| AI 辅助实测 | **13.2 人时**（含读需求、grill-me 全段、2 次返工） |
| 工时节省率 | **20.0%**（全周期上报口径；编码切片 63.6% 已废弃、不上报） |
| 返工 | **2 次**：布局改 `tr/td`；Tax&Fee 按产品合并逻辑重做弹层 |
| 工具建设投入 | **（待第 2.2 节实填）**，一次性，摊薄到后续需求 |
| 质量 | 15 场景 0 错误，可判定分区覆盖 100%，2 个未决分支已人工确认 |
| 流程沉淀 | 自测与提 MR 环节已工具化，下一个需求可直接复用 |
| 局限 | 对照为「本人估时 vs AI 路径」，非第二人盲做；工时按对话与落盘还原而非秒表；静态自测不等于真实渲染验证 |

**一句话上报：**

> 本案例按全周期记账（读需求 + grill-me + 编码 + 自测）：手写估时 16.5h，AI 路径实测 13.2h，节省 20%，含 2 次返工。
> 编码段确实更快，但需求理解和口径锁定几乎不省，整单不能按编码切片报。
> 质量：15 组场景 0 错误、可判定分区覆盖 100%；自测与提 MR 已沉淀成可复用工具。

---

## 5. 下一步：真正的效率验证在第二次

本次数字的最大局限是**工具是边做边建的**，且 grill-me 是第一次完整走，口径锁定偏长。

真正能说明问题的是**第二个同类需求**：工具已就位，grill 也有前案可抄，那次的工时才是稳态效率。
建议下一个小票需求按同一套工作包记账（仍然把读需求和 grill-me 单列），两次对比出来的曲线比单点数字有说服力得多。

配合验证的还有一件事：**让另一位同事按 KR1 的复用步骤独立跑一次**。
如果他不用问就能跑通，说明沉淀是有效的；如果卡住，卡在哪里就是文档要补的地方。

---

## 6. 可复用的采集方法

1. 开工前按工作包拆手写估时，不要事后拍脑袋
2. AI 路径按同一工作包记账，不要混成一笔「总共用了半天」
3. **工具建设与需求交付分开记**，否则一次性投入会污染效率结论
4. **读需求、grill-me 必须单列**，不要塞进「对齐模块」一行里；否则编码切片会虚高
5. 返工次数单独计，避免「看起来快、实际修很久」被掩盖
6. 质量数据要可复现，报告里附上能重跑的命令
7. 报告固定五句话：样本是什么、手写多少、AI 多少、质量如何、局限一句
8. 上报用全周期节省率，编码切片最多当内部对照

---

## 7. KR3 完成定义（DoD）

- [x] 指标已定义（效率类 + 质量类）
- [x] 质量基线已实测且可复现
- [x] 采集表已按「需求 / 工具」两笔账重构
- [x] 第 2.1 节已按全周期重填（含需求理解 + grill-me），上报节省率 20%
- [ ] 第 2.2 节工具建设工时填完
- [ ] 结论同步到 OKR 系统进展/附件（按 20% 口径，不要贴旧的 63.6%）
- [ ] 第二个同类需求按同口径记账，出稳态效率对比
