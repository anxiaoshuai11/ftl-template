# KR2 · 完成实践文档输出，满足组内默认标准落地

> 归属 OKR **O2**：完成前端 AI 样板间核心能力落地，支撑组内规范沉淀与效率基线测算（截止 09-30）  
> 权重：33.3%  
> 案例来源：短信 H5 合并小票（见 [KR1-可复用案例.md](./KR1-可复用案例.md)）

**本 KR 交付物：** 组内可当默认标准的实践文档（工作流 / Checklist / 提示词 / 踩坑）。

配套文档：[KR1-可复用案例.md](./KR1-可复用案例.md) · [KR3-效率基线报告.md](./KR3-效率基线报告.md)

---

## 1. 推荐默认工作流（本组可直接当标准）

```text
需求/UI 齐套
  → 锁定口径（交付物形态、数据契约、砍留模块）   // 可用 grill / 评审纪要
  → 准备输入包（旧 FTL + JSON 文档 + UI 图）
  → AI 生成骨架（HTML/CSS + 模块分区）
  → AI/人工绑定 FreeMarker 与边界 #if
  → Demo JSON 自测（空数据、OO 胶囊、多支付、弹层）
  → 补实践文档与工时采集（对齐 KR2 / KR3）
  → 交后端联调真数据
```

同类需求（旧模版合并、H5 换皮、JSON 拼盘占位）默认按此流程走，不要一上来就整段粘贴旧 FTL。

---

## 2. 组内默认标准 Checklist（做同类需求请勾）

### 开始前

- [ ] 写清：打印小票 vs 短信 H5 是否同一套模版（本案例：**不是**，H5 新建）
- [ ] 写清：数据是「新 DTO」还是「旧 JSON 拼盘」（本案例：**拼盘占位**）
- [ ] 列出「必显模块 / 必砍模块」，避免从旧 FTL 整段粘贴

### 生成中

- [ ] 提示词写明禁止项（渠道、条码、刷卡联等）
- [ ] 变量命名完整，关键分支加中文注释，不写 `@link`
- [ ] 交互（如弹层）用原生 JS，避免重依赖

### 交付前

- [ ] Demo JSON 可单独渲染
- [ ] 覆盖空数据：无顾客 / 无支付 / 无 logo / 无 Tax 明细
- [ ] 文档含：复用步骤 + 提示词 + 字段表 + 工时表

---

## 3. 可复用 AI 提示词

```text
你是前端同学。请基于已锁定口径生成「短信 H5 合并小票」FreeMarker 模版，不要热敏 576 打印风格。

输入：
1) 订单小票 FTL + JSON 数据模型
2) 交易小票 FTL + JSON 数据模型
3) UI：移动端白底收据；分区虚线；仅英文

必须包含模块：门店、单头、顾客(有则显)、餐品、金额、支付(无则整块藏)。
单头胶囊规则：有桌台只显示 diningOption；OO(orderChannel=1)且无桌台且有 confirmationNumber 时显示「类型 / 取餐码」；不显示渠道；不拼桌台。
金额：orderAmountInfo；Tax&Fee 若有 taxFeeDetailInfo 则显示 i 弹层（原生 JS）。
支付：只用 tradeInfo 多支付行。
不要：渠道、评价、条码、小费勾选框、刷卡价现金价、AID/签名刷卡联、transInfo 展示。
变量命名规范，关键逻辑加中文注释，不要 @link。
另附一份拼盘 demo JSON（订单字段为主，交易字段可保留但不强制渲染）。
```

使用约定：

1. 先锁定口径（交付物、砍留、字段），再贴提示词，避免 AI 从旧 FTL 抄过头
2. 输入包必须同时给：旧模版 + JSON 文档 + UI 图
3. 生成后用人过一遍 Checklist，不要直接当终稿

---

## 4. 踩坑备忘（建议写进组规范）

1. 旧订单 FTL 渠道矩阵很重，H5 需求不显示渠道——AI/人都容易从旧模版抄过头。
2. `orderNumber` 有的环境是字符串，有的是 `{content:[]}`——模版需兼容。
3. Tax&Fee 明细旧 JSON 可能没有——用 `taxFeeDetailInfo` 占位，无数据隐藏 `i`。
4. 交易 JSON 的 tipType / 签名是商户联能力，顾客短信 H5 默认不要。
5. 图稿和口头口径可能不一致（本案例：图上胶囊带桌台，产品确认只要类型）——以锁定口径为准，不要盲跟图。

---

## 5. 口径锁定模板（下次可直接填）

| 决策点 | 本案例结论 | 下次填写 |
|--------|------------|----------|
| 交付物形态 | 一个新 FTL（H5，非热敏） | |
| 数据契约 | 旧 JSON 拼盘占位，不调接口 | |
| 支付深度 | 只用 `tradeInfo`，不做刷卡联 | |
| 无支付 | 整块不渲染 | |
| 弹层 | 有 `taxFeeDetailInfo` 才做 | |
| 语言 | 仅英文 | |

---

## 6. KR2 完成定义（DoD）

- [x] 有工作流 + checklist + 提示词 + 踩坑
- [ ] 同步到组内默认文档库/飞书（需发布）
- [ ] 组内评审通过「可当默认标准」一票（需组织）
