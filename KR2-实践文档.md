# KR2 · 完成实践文档输出，满足组内默认标准落地

> 归属 OKR **O2**：完成前端 AI 样板间核心能力落地，支撑组内规范沉淀与效率基线测算（截止 09-30）
> 权重：33.3%
> 案例来源：订单+交易合并小票（见 [KR1 可复用案例](https://zath6evqnvz.feishu.cn/wiki/ATglwvtpAidD85kHEjfcPkUQnVe)）

**本 KR 交付物：** 组内可当默认标准的实践文档，**且这套标准是可执行的，不只是纸面约定**。

配套文档：[KR1 可复用案例](https://zath6evqnvz.feishu.cn/wiki/ATglwvtpAidD85kHEjfcPkUQnVe) · [KR3 效率基线](https://zath6evqnvz.feishu.cn/wiki/HRmAwIkjEiHzXfk31THcnzU0n0c)

---

## 0. 标准靠什么保证执行

**写成条目让人自觉去勾，赶工期时会第一个被牺牲；写成工具就不会。**

所以这套标准的关键环节都落成了三个 Cursor skill 加一套脚本，
放在 [Shawn/ftl-util](http://gitlab.zbspos.com/Shawn/ftl-util)：

| 要保证的事 | 靠什么保证 |
|------------|------------|
| 覆盖空数据场景 | 场景套件跑 15 组，报告直接列出「哪个条件从没为真过」 |
| 自测完再提 MR | skill 明令禁止跳过自测，自测有退出码可卡 |
| 别改坏其他分区 | 回归对比工具，改现有模版必跑，只报这次引入的问题 |
| 分支名符合目标仓库规范 | 脚本读目标仓库最近的分支自己统计格式 |
| MR 里写清没测到什么 | MR 描述模板固定含「自测覆盖不到的部分」一节 |
| 过程材料不污染仓库 | 报告走 GitLab 附件区上传，MR 变更文件只有 `.ftl` |

---

## 1. 默认工作流

```text
飞书需求文档
  → 读需求，落成书面改动清单（分区 / 改动 / 字段 / 无数据时的表现）
  → 跟需求方对齐口径，有歧义必须问，不猜
  → 编码
  → 自测出报告（改现有模版先跑回归对比）
  → 出 HTML 预览，人眼看样式
  → 按目标仓库习惯建分支
  → 提 MR，自测报告走附件区上传，不入库
```

在 Cursor 里就是一句话，三样输入：

```
按流程做这个需求：<飞书文档链接>
模版：<你的.ftl>
数据：<你的.json>
目标仓库：<你本地的 print-util 路径>
```

### 开工前必须问清的三件事

不要猜，猜错了整条链路白跑：

1. **目标仓库路径** —— FTL 最终落到哪个仓库哪个目录
2. **是改现有模版还是新增** —— 决定要不要做回归对比，这是最关键的分叉
3. **配套 JSON 从哪来** —— 仓库里只有 FTL 没有数据样例，改现有模版必须要到真实 JSON，
   否则自测只是走过场

---

## 2. 改动清单模板（读完需求先填这张表）

| 分区 | 改动 | 数据来源字段 | 无数据时的表现 |
|------|------|--------------|----------------|

**最后一列最容易被需求文档略过，也最容易上线出事。** 是整块隐藏、显示占位、还是显示 0，
必须确认到没有歧义再动手。本案例就踩过一次：图稿上胶囊带桌台号，产品口径确认是只要类型。

---

## 3. 交付前 Checklist

工具能自动保证的部分已经不用勾了，这里只留人必须判断的：

- [ ] 报告里的**错误级问题全部清零**（告警逐条确认是不是误报）
- [ ] 报告里「用例还缺的顶层分支」章节**已清空**（不够就补场景）
- [ ] 「判不了」的分支**已人工看预览确认**，没有当成「测过了」
- [ ] 改现有模版：回归对比**引入 0 个新问题**，新增分支已补对应用例
- [ ] HTML 预览已生成并人眼看过样式
- [ ] MR 描述含：需求链接、自测结论、**自测覆盖不到的部分**、需评审拍板的业务口径
- [ ] MR 变更文件列表里**只有 `.ftl`**（`git diff --name-only main..<分支>` 核一遍）

---

## 4. 自测能测什么、不能测什么

本地没有 Java 和 FreeMarker 引擎，自测是**静态推演**，不是真实渲染。写报告和汇报时必须如实说明。

**能测**（这三类都是要等真数据渲染那一刻才炸的）：

- **空值风险** —— `${xxx}` 取不到值又没写 `!默认值`，运行时抛 `InvalidReferenceException`
- **数组越界** —— `content[1]` 但实际数据只有 1 项
- **指令闭合** —— `<#if>` / `<#list>` 没配对

**测不到**：真实引擎渲染结果、视觉样式（断行/对齐/溢出/弹层位置）、
邮件客户端兼容性（Outlook 走 Word 引擎、Gmail 剥离 `<style>`）、短信通道与短链跳转、业务口径。

条件里调用了自定义 `#function` 的分支，静态分析给不出结论，报告会标成
**「判不了」并单列一节，不计入覆盖率分母**。这是工具的能力边界，不是用例没写够。

---

## 5. 可复用 AI 提示词

优先用 skill（一句话三样输入）。下面这段用于 skill 覆盖不到的场景，或者想手工控制生成过程时。

```text
你是前端同学。请基于已锁定口径生成「订单+交易合并小票」FreeMarker 模版，不要热敏 576 打印风格。

输入：
1) 订单小票 FTL + JSON 数据模型
2) 交易小票 FTL + JSON 数据模型
3) UI：移动端白底收据；分区虚线；仅英文

必须包含模块：门店、单头、顾客(有则显)、餐品、金额、支付(无则整块藏)。
单头胶囊规则：有桌台只显示 diningOption；OO(orderChannel=1)且无桌台且有 confirmationNumber
时显示「类型 / 取餐码」；不显示渠道；不拼桌台。
金额：orderAmountInfo；Tax&Fee 若有 taxFeeDetailInfo 则显示 i 弹层（原生 JS）。
支付：只用 tradeInfo 多支付行。
不要：渠道、评价、条码、小费勾选框、刷卡价现金价、AID/签名刷卡联、transInfo 展示。
变量命名规范，关键逻辑加中文注释，不要 @link。
另附一份拼盘 demo JSON（订单字段为主，交易字段可保留但不强制渲染）。
```

使用约定：

1. 先锁定口径（交付物、砍留、字段），再贴提示词，避免 AI 从旧 FTL 抄过头
2. 输入包必须同时给：旧模版 + JSON 文档 + UI 图
3. 生成后必须跑自测，不要直接当终稿

---

## 6. 踩坑备忘

**需求与实现层面：**

1. 旧订单 FTL 渠道矩阵很重，H5 需求不显示渠道——AI 和人都容易从旧模版抄过头
2. `orderNumber` 有的环境是字符串，有的是 `{content:[]}`——模版需兼容
3. Tax&Fee 明细旧 JSON 可能没有——用 `taxFeeDetailInfo` 占位，无数据隐藏 `i`
4. 交易 JSON 的 tipType / 签名是商户联能力，顾客短信 H5 默认不要
5. 图稿和口头口径可能不一致——以锁定口径为准，不要盲跟图

**流程与工程层面：**

6. **分支命名规范是按仓库走的，不是按团队走的。** `print-util` 用 `需求名_日期_作者`，
   `posbee-h5-admin` 用 `需求名_作者_日期`，照搬会格格不入。作者段还是个人代号
   （`xiaowh` / `jany` / `wjh`），不是 `git config user.name` 的全名。
7. **自测报告不要提交进模版仓库。** 它是过程材料，走 GitLab 附件区上传，
   评审点开能看、但不产生提交，MR 变更文件列表保持只有 `.ftl`。
8. **生成报告时输出路径要指到目标仓库外面**，否则默认写在模版旁边，等于污染人家工作区。
9. **改现有模版只看「改完还剩几个问题」没有意义**，要看「哪些问题是这次改出来的」。
   老模版本来就带着的告警不该拦你，你新写出来的空值风险必须拦你。
10. **群组仓库的默认分支受保护，Developer 推不了空仓库的第一次提交。**
    建仓前先确认自己在目标群组是什么角色，否则建完推不上去。

---

## 7. 口径锁定模板（下次直接填）

| 决策点 | 本案例结论 | 下次填写 |
|--------|------------|----------|
| 交付物形态 | 一个新 FTL（H5，非热敏） | |
| 新增还是改现有 | 新增，不动现有热敏模版 | |
| 数据契约 | 旧 JSON 拼盘占位，不调接口 | |
| 支付深度 | 只用 `tradeInfo`，不做刷卡联 | |
| 无支付时 | 整块不渲染 | |
| 弹层 | 有 `taxFeeDetailInfo` 才做 | |
| 语言 | 仅英文 | |
| 目标仓库与目录 | `print-util` 的 `src/main/resources/templates/` | |

---

## 8. KR2 完成定义（DoD）

- [x] 有工作流 + checklist + 提示词 + 踩坑备忘
- [x] **标准已工具化**，关键环节由脚本与 skill 保证，不依赖个人自觉
- [x] 工具与 skill 已发布，同事 clone 即用（[Shawn/ftl-util](http://gitlab.zbspos.com/Shawn/ftl-util)）
- [ ] 同步到组内默认文档库（需发布）
- [ ] 组内评审通过「可当默认标准」一票（需组织）
- [ ] 至少 1 位同事实跑一次并反馈卡点（验证标准是否真的可执行）
